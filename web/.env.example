# Backend API base URL the browser talks to directly.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Optional: proxy backend calls through Next (docker-compose uses this)
# API_PROXY_TARGET=http://api:8080
